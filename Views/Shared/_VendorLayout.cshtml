@* Views/Shared/_VendorLayout.cshtml *@
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>@ViewData["Title"] - Vendor</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet" />

    <style>
        body {
            font-family: 'Segoe UI', sans-serif;
            background-color: #f8f9fa;
            margin: 0;
            padding: 0;
        }

        .sidebar {
            position: fixed;
            top: 0;
            left: 0;
            height: 100vh;
            width: 70px;
            background-color: white;
            border-right: 1px solid #dee2e6;
            transition: width 0.3s ease;
            overflow-x: hidden;
            z-index: 1001;
        }

            .sidebar:hover {
                width: 250px;
            }

            .sidebar .sidebar-logo {
                height: 60px;
                background-color: dodgerblue;
                display: flex;
                align-items: center;
                justify-content: center;
            }

        .sidebar-logo img {
            width: 40px;
            height: 40px;
            border-radius: 50%;
        }

        .sidebar .nav-link {
            color: #333;
            padding: 15px 20px;
            white-space: nowrap;
            display: flex;
            align-items: center;
            transition: background 0.2s ease;
        }

            .sidebar .nav-link i {
                font-size: 1.3rem;
                margin-right: 12px;
            }

            .sidebar .nav-link span {
                opacity: 0;
                transition: opacity 0.3s ease;
            }

        .sidebar:hover .nav-link span {
            opacity: 1;
        }

        .topbar {
            position: fixed;
            top: 0; /* Add this */
            width: calc(100% - 70px);
            left: 70px;
            height: 70px;
            background-color: white;
            border-bottom: 1px solid #dee2e6;
            padding: 10px 20px;
            z-index: 1000;
            display: flex;
            align-items: center;
            justify-content: space-between;
            transition: left 0.3s ease, width 0.3s ease;
        }

        .sidebar:hover ~ .topbar {
            left: 250px;
            width: calc(100% - 250px);
        }

        .topbar h5 {
            color: dodgerblue;
            font-size: 1.25rem;
            font-weight: 600;
        }

        .main-content {
            margin-left: 70px;
            margin-top: 90px; /* Matches height of topbar + spacing */
            padding: 30px;
            transition: margin-left 0.3s ease;
        }


        .sidebar:hover ~ .main-content {
            margin-left: 250px;
        }

        .main-content .container-fluid {
            max-width: 2000px;
            margin-top: 70px;
        }

        .dropdown-menu {
            min-width: 140px;
        }
    </style>
</head>
<body>
    <!-- Sidebar -->
    <div class="sidebar">
        <div class="sidebar-logo">
            <img src="~/images/Logo4.png" alt="Logo" />
        </div>

        <a asp-controller="Vendor" asp-action="Dashboard" class="nav-link">
            <i class="bi bi-journals"></i>
            <span>Open RFQs</span>
        </a>
        <a asp-controller="Vendor" asp-action="MySubmittedQuotations" class="nav-link">
            <i class="bi bi-upload"></i>
            <span>My Submissions</span>
        </a>
        <a asp-controller="Vendor" asp-action="MyFinalizedQuotations" class="nav-link">
            <i class="bi bi-check-circle"></i>
            <span>Finalized Quotations</span>
        </a>
    </div>

    <!-- Topbar -->
    <div class="topbar">
        <h5 class="mb-0">@ViewData["Title"]</h5>
        <div class="d-flex align-items-center gap-3 position-relative">
            <i class="bi bi-bell fs-5"></i>
            <div class="dropdown">
                <a class="nav-link dropdown-toggle p-0" href="#" id="vendorDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                    <img src="/images/User.png" alt="User" class="rounded-circle" width="35" height="35" />
                </a>

                <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="vendorDropdown">
                    <li><a class="dropdown-item" asp-controller="Auth" asp-action="Logout">Logout</a></li>
                </ul>
            </div>
        </div>
    </div>

    <!-- Main Content -->
    <div class="main-content">
        @RenderBody()
    </div>

    @RenderSection("Scripts", required: false)
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
